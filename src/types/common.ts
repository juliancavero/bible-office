export type ProviderProps = {
  children: React.ReactNode;
};

export type DateType = {
  year: string;
  month: string;
  day: string;
};

export type MissingDates = {
  day: number;
  month: number;
};
